@model IEnumerable<STEF.Models.Takmicenje>

@{
    ViewBag.Title = "Index";
}

<h2>Takmicenje</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table" style="border:2px solid black;">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Naziv)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Grupa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Naziv)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Grupa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TakmicenjeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TakmicenjeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TakmicenjeID })
        </td>
    </tr>
}

</table>
